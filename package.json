{
  "name": "insect-alert",
  "version": "0.1.0",
  "private": true,
  "description": "Detecteer insect-ingrediënten in voedingsmiddelen — paste of foto",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@google/genai": "^1.51.0",
    "browser-image-compression": "^2.0.2"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
